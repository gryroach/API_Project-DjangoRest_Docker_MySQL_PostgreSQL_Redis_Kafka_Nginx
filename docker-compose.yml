version: '3.8'

services:
  web:
      build: ./drf_project
      command: python manage.py runserver 0.0.0.0:8000
      volumes:
        - ./drf_project/:/usr/src/drf_project/
      ports:
        - "8000:8000"
      env_file:
        - ./.env
      depends_on:
        - db
  db:
    image: mysql:5.7
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_db:/var/lib/mysqldata
    environment:
      MYSQL_DATABASE: db_django
      MYSQL_PASSWORD: root
      MYSQL_ROOT_PASSWORD: root
    restart: always